# Task: Refactor CLI docstrings for Sphinx + Napoleon

Refactor each Click command's docstring and signature to be compatible with Sphinx autodoc and
Napoleon (Google style). Ensure accurate type hints and rich, actionable docstrings.

## Acceptance Criteria
- Google-style docstrings with Args, Returns, Raises, and Examples as applicable.
- Function signatures include precise type hints for all parameters and return types.
- No TODO placeholders remain in docstrings within this module.
- make html (or Sphinx build) renders cleanly without Napoleon warnings for these functions.

## Checklist
- [ ] `cmd_task_docs_refactor` (`task-docs-refactor`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_init_db` (`init-db`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_migrate_db` (`migrate-db`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_create_advertiser` (`create-advertiser`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_create_campaign` (`create-campaign`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_generate_performance` (`generate-performance`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_generate_performance_ext` (`generate-performance-ext`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_create_example` (`create-example`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_test_fields` (`test-fields`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_create_profile` (`create-profile`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_test_scenario` (`test-scenario`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_list_campaigns` (`list-campaigns`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_export_campaign` (`export-campaign`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_compare_campaigns` (`compare-campaigns`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_compare_by_objective` (`compare-by-objective`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_optimize_cpm` (`optimize-cpm`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_project_roi` (`project-roi`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_test_creative` (`test-creative`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_ab_test` (`ab-test`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_forecast` (`forecast`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_seasonal_trends` (`seasonal-trends`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_create_variations` (`create-variations`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_list_templates` (`list-templates`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_show_schemas` (`show-schemas`) — Add/verify Google-style docstring and type hints.
- [ ] `cmd_status` (`status`) — Add/verify Google-style docstring and type hints.
